const SectionTitle = ({ text }) => {
  return (
    <div className="border-b border-gray-200 pb-5">
      <h2 className="tracking-wider capitalize text-3xl font-medium">{text}</h2>
    </div>
  );
};

export default SectionTitle;
